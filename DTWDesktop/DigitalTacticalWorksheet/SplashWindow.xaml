<Window x:Class="DigitalTacticalWorksheet.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Topmost="True">

    <Border CornerRadius="12" Background="#222" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Image Grid.Row="0"
                   Source="Splash.png"
                   Stretch="None"/>

            <StackPanel Grid.Row="1" Margin="0,15,0,0">
                <TextBlock Text="Created by Daniel Romano"
                           Foreground="White"
                           FontSize="14"
                           Opacity="0.85"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="© 2025"
                           Foreground="White"
                           FontSize="12"
                           Opacity="0.65"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
